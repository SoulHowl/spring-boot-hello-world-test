name: Test Project

on:
  push:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Deploy to Render
      uses: JorgeLNJunior/render-deploy@v1.4.3
      with:
        service_id: srv-cnio1ui1hbls73fjcpq0?key=KxEyqrcRlCs
        api_key: rnd_Hd2RJkLlueEDIRWdg41qLPyG9TRF

